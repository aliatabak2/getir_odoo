<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Getir Siparişlerini Otomatik Çekme Cron Job - Her dakika çalışır -->
        <record id="ir_cron_getir_fetch_orders" model="ir.cron">
            <field name="name">Getir: Siparişleri Otomatik Çek</field>
            <field name="model_id" ref="getir_connector.model_getir_api"/>
            <field name="state">code</field>
            <field name="code">model._cron_fetch_orders()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">minutes</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="priority">5</field>
        </record>
    </data>
</odoo>
